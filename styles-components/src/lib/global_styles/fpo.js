import { render, Artboard, Text, View, StyleSheet } from 'react-sketchapp';

let toggle = 'rgba(0,0,0,0.0)';

export const fpo = StyleSheet.create({
  placeholder:{
    backgroundColor: toggle,
  }
});
